<?php
$category = $this->getCategory();
?>
<div class="box">
    <div class="container">
        <div class="title">Category Information</div>
        <div class="content">
            <form action=<?php echo $this->getUrl('admin/catalog_category/save') ?> method="post">
                <input type="hidden" id="category_id" name="catalog_category[category_id]"
                    value="<?php echo $category->getCategoryId() ?>">

                <div class="user-details">
                    <div class="form-group">
                        <label for="name" class="details">Name </label>
                        <input type="textbox" class="form-control" id="name" name="catalog_category[category_name]"
                            value="<?php echo $category->getCategoryName() ?>" placeholder="Enter name">
                    </div>
                    <div class="form-group">
                        <label for="status" class="details">Status</label>
                        <select id="status" name="catalog_product[status]" value="<?php echo $category->getStatus() ?>"
                            class="form-control">
                            <?php
                            $status = ["-- select --", "Enabled", "Disabled"];
                            foreach ($status as $item) {
                                echo '<option value="' . $item . '" ' . (($category->getStatus() == $item) ? 'selected' : '') . '>' . $item . '</option>';
                            }
                            ?>
                        </select>
                    </div>
                </div>
                <div class="button">
                    <input type="submit" value="Submit" name="submit">
                </div>
            </form>
        </div>
    </div>
</div>